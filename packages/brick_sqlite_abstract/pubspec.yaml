name: brick_sqlite_abstract
description: Annotations and other support for Brick SQLite that does not rely on dart:mirrors
homepage: https://github.com/GetDutchie/brick/tree/main/packages/brick_sqlite_abstract
issue_tracker: https://github.com/GetDutchie/brick/issues
repository: https://github.com/GetDutchie/brick

version: 1.1.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  collection: ">=1.15.0 <2.0.0"
  brick_core: ^1.1.1
  meta: ">=1.3.0 <2.0.0"

dev_dependencies:
  lints: ^1.0.1
  test: ^1.16.5
