name: brick_offline_first_with_rest_example

publish_to: none

environment:
  sdk: ">=2.18.0 <4.0.0"

dependency_overrides:
  brick_offline_first_with_rest:
    path: ../
  brick_rest_generators:
    path: ../../brick_rest_generators
  brick_offline_first_build:
    path: ../../brick_offline_first_build
  brick_offline_first_with_rest_build:
    path: ../../brick_offline_first_with_rest_build

dev_dependencies:
  build_runner:
  build_resolvers:
  brick_offline_first_with_rest_build:
