name: Brick SQLite
on:
  push:
    branches:
      - main
  pull_request:
    paths:
      - "packages/brick_sqlite/**"
      - ".github/workflows/brick_sqlite.yaml"

env:
  PUB_ENVIRONMENT: bot.github

jobs:
  analyze_format_test:
    uses: GetDutchie/brick/.github/workflows/reusable-flutter-analyze-format-test.yaml@main
    with:
      package: brick_sqlite
