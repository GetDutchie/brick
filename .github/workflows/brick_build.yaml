name: Brick Build
on:
  push:
    branches:
      - main
  pull_request:
    paths:
      - "packages/brick_build/**"
      - ".github/workflows/brick_build.yaml"

defaults:
  run:
    shell: bash

env:
  PUB_ENVIRONMENT: bot.github

jobs:
  analyze_format_test:
    uses: GetDutchie/brick/.github/workflows/reusable-dart-analyze-format-test.yaml@main
    with:
      package: brick_build
